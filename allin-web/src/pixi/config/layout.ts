// ── 画布 ─────────────────────────────────────────────
/** 画布总宽度（16:9，适配 PC / macOS 浏览器） */
export const CANVAS_W = 1600
/** 画布总高度 */
export const CANVAS_H = 900

// ── 牌桌椭圆 ─────────────────────────────────────────
/** 椭圆中心 X（画布水平居中） */
export const TABLE_CX = 800
/** 椭圆中心 Y（偏上，给底部本地玩家留空间） */
export const TABLE_CY = 385
/** 椭圆 X 半轴（毛毡外边缘） */
export const TABLE_RX = 555
/** 椭圆 Y 半轴（毛毡外边缘） */
export const TABLE_RY = 220
/** 木质边框厚度（变得更细莹） */
export const RAIL_W = 17

// ── 座位 ─────────────────────────────────────────────
/** 本地玩家头像半径（底部大头像） */
export const AVATAR_R_LOCAL = 52
/** 远端玩家头像半径 */
export const AVATAR_R_REMOTE = 38

/**
 * 9 个座位中心坐标（显示索引 0 = 本地玩家，顺时针排列）
 *
 * 分布：底部 2（本地在右）→ 右侧 2 → 顶部 3 → 左侧 2
 *   0  底部右（本地）
 *   1  右侧下
 *   2  右侧上
 *   3  顶部右
 *   4  顶部中
 *   5  顶部左
 *   6  左侧上
 *   7  左侧下
 *   8  底部左
 */
export const SEAT_POSITIONS: { x: number; y: number }[] = [
  { x: 1020, y: 715 },  // 0  底部右（本地玩家，大头像）
  { x: 1355, y: 525 },  // 1  右侧下
  { x: 1355, y: 245 },  // 2  右侧上
  { x: 1120, y: 90  },  // 3  顶部右
  { x: 800,  y: 80  },  // 4  顶部中
  { x: 480,  y: 90  },  // 5  顶部左
  { x: 245,  y: 245 },  // 6  左侧上
  { x: 245,  y: 525 },  // 7  左侧下
  { x: 580,  y: 715 },  // 8  底部左
]

// ── 牌桌纹理 (NineSlice) ───────────────────────────────
/**
 * 九宫格牌桌纹理生成的参数配置
 */
export const TABLE_TEX_CONFIG = {
  /** 纹理基础矩形的大小（增大基础画布以支持更大的圆角半径） */
  SIZE: 600,
  /** 牌桌圆角半径，加大此值可让牌桌两端显得更圆（280已经能达到标准牌桌高度的近乎正半圆形） */
  CORNER_RADIUS: 280,
} as const
